#ifndef EULER_HPP
#define EULER_HPP
#include <vector>
#include <utility>

std::vector<size_t> euler_check_graph(const size_t n, const std::vector<std::pair<size_t, size_t>>& edges);

#endif // EULER_HPP